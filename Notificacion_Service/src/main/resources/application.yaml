spring:
  application:
    name: Notificacion

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


datasource:
  url: ${SPRING_DATASOURCE_URL}
  username: ${SPRING_DATASOURCE_USERNAME}
  password: ${SPRING_DATASOURCE_PASSWORD}


server:
  port: 9094
